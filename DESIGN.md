# Trace 设计规范

本文件是桌面 UI 的验收基线。延续当前配色与紧凑 macOS Pro 方向，解决组件之间的样式漂移。对应需求 OPC-60。共享实现入口为 `packages/ui/src/styles.css`，桌面布局为 `apps/desktop/src/app.css`，模块样式为 `accounts.css`、`mcp.css`、`updates.css`。

## 1. 设计方向与范围

面向长期管理 Skill、工作流和 AI 环境的专业用户。界面安静、紧凑、可检查，突出当前工作内容。保留深浅双主题、原生字体、钴蓝主操作和天蓝辅助强调，保留模块导航与业务能力；按第8节优化页面内部信息层级和操作位置，不改变业务语义。

毛玻璃只用于窗口结构表面，采用 `blur(40px) saturate(180%)`；内容面板用明确明度层次和微弱 1px 边界。禁止装饰渐变、发光、厚阴影和重复嵌套卡片。品牌 Logo 保留官方颜色，不随语义 token 强制重染。

## 2. 主题与语义色

以下 OKLCH 值来自当前共享主题，不使用不准确的十六进制近似。组件必须引用语义变量，禁止单独硬编码深色背景、白色文字或红黄绿状态色。

| Token | 深色 | 浅色 |
| --- | --- | --- |
| `--color-bg` | `oklch(0.120 0.004 250)` | `oklch(0.985 0.002 250)` |
| `--color-canvas` | `oklch(0.145 0.005 250)` | `oklch(1 0 0)` |
| `--color-surface` | `oklch(0.175 0.006 250)` | `oklch(0.965 0.004 250)` |
| `--color-surface-raised` | `oklch(0.215 0.008 250)` | `oklch(0.925 0.007 250)` |
| `--color-rail` | `oklch(0.160 0.006 250)` | `oklch(0.940 0.005 250)` |
| `--color-ink` | `oklch(0.960 0.003 250)` | `oklch(0.160 0.010 250)` |
| `--color-muted` | `oklch(0.620 0.010 250)` | `oklch(0.480 0.015 250)` |
| `--color-subtle` | `oklch(0.440 0.010 250)` | `oklch(0.640 0.010 250)` |
| `--color-border` | `oklch(0.260 0.007 250)` | `oklch(0.890 0.006 250)` |
| `--color-primary` | `oklch(0.580 0.200 250)` | `oklch(0.520 0.210 250)` |
| `--color-accent` | `oklch(0.780 0.130 210)` | `oklch(0.580 0.160 215)` |
| `--color-success` | `oklch(0.760 0.160 155)` | `oklch(0.420 0.140 155)` |
| `--color-waiting` | `oklch(0.780 0.150 75)` | `oklch(0.460 0.140 75)` |
| `--color-danger` | `oklch(0.650 0.220 25)` | `oklch(0.550 0.200 25)` |
| `--control-bg` | `oklch(0.110 0.004 250)` | `oklch(0.910 0.008 250)` |
| `--control-bg-hover` | `oklch(0.125 0.005 250)` | `oklch(0.890 0.010 250)` |
| `--control-bg-focus` | `oklch(0.095 0.004 250)` | `oklch(0.925 0.007 250)` |
| `--control-border` | `oklch(0.260 0.007 250)` | `oklch(0.830 0.010 250)` |
| `--control-placeholder` | `oklch(0.580 0.010 250)` | `oklch(0.460 0.015 250)` |

- 主操作使用 `--btn-primary-*`；次操作使用 `--btn-secondary-*`。蓝色只用于操作、选择和焦点。
- 成功/等待/危险提示成组使用 `--color-<state>-bg`、`--color-<state>-ink`；边界使用相应语义色的低透明度版本。
- `--color-subtle` 只用于非必要装饰，不用于正文、表单标签或占位文字。正文和占位文字对比度目标 4.5:1；大字与必要非文字状态目标 3:1。表内既有值若不达标，只调整对应语义文字 token 的明度并同步此表，不更换色相方向。
- 新的兼容别名必须指向已有语义 token，不建立第二套色板。官网继承共享主题，需检查兼容性，但不纳入本次页面重设计。

## 3. 布局

- 业务工作台基线为 `160px 210px minmax(0, 1fr)`。Sidebar 固定 160px，Master 默认 210px；保留当前已实现的用户拖拽列宽及持久化行为，不因视觉修复删除功能。
- 设置模式为 `160px minmax(0, 1fr)`，隐藏 Master，主舞台全宽承载设置内容。保留已有设置导航项。
- 主舞台常规水平内边距 16px，设置页面 28px；顶部保留当前 38px 窗口拖拽安全区，底部 24px，设置底部 40px。
- 长内容在所属区域滚动；flex/grid 子项使用 `min-width: 0`，长路径省略或在详情换行。窄窗口允许内部表单改单列，不擅自隐藏业务栏或改变导航架构。

### 3.1 滚动条不可见（强制）

**应用内禁止出现任何可见滚动条，包括横向、纵向、原生、覆盖式和自定义滚动条。** 深浅主题、默认、悬停、聚焦及正在滚动时均不得显示滚动条轨道或滑块，也不得预留滚动条空槽。

- 覆盖主窗口、侧栏、列表、详情、设置、弹窗、独立子窗口、代码块、textarea、可编辑区域及第三方编辑器内部滚动容器。
- 长内容保留 `overflow: auto` 等必要滚动能力，支持滚轮、触控板、触屏以及适用的键盘滚动；聚焦控件和编辑器光标移出视口时应正常滚入可见区域。禁止通过 `overflow: hidden` 或 `clip` 截断需要访问的内容来隐藏滚动条。
- 实现统一使用 `scrollbar-width: none`，并为 Chromium/WebKit 的 `::-webkit-scrollbar` 设置 `display: none; width: 0; height: 0`；不使用 `scrollbar-gutter: stable` 预留空槽。第三方组件自行绘制的轨道与滑块也必须关闭。
- 滚动容器仍须可通过键盘到达；长列表、长文档和图谱的末尾内容必须可访问。保持既有滚动位置、选择、光标与窗口拖拽行为。

## 4. 尺寸与间距

| 项目 | 标准 |
| --- | --- |
| 导航 `.nav-pill-btn` | 26px 高，水平 padding 8px |
| 标准 `.btn` / `.btn--capsule` | 24px 高，水平 padding 10px |
| 小号 `.btn--sm` | 22px 高，水平 padding 8px |
| Master 分段 Tab 外框与按钮 | 均 24px 高；外框不得再叠加使子项溢出的垂直 padding/border |
| 单行输入、搜索、下拉 | 28px 高，水平 padding 10px |
| 命令搜索 | 28px 高，与其他搜索一致 |
| 多行编辑 | 高度按内容场景，padding 12px 16px；不套用 28px 限高 |
| 普通列表行 | 默认最小 48px；纯导航按 26px |
| 图标 | 普通 14px，紧凑 12px，导航 16px；品牌标识按现有容器 |
| 圆角 | 小 6px，控件 8px，卡片 10px，面板最大 12px，胶囊全圆 |

间距主刻度使用 `--space-1/2/3/4/6/8/12`，对应 4/8/12/16/24/32/48px。图标到文字和紧密工具组允许 6px，字段水平 padding 允许 10px。1/2px 仅用于描边或内部对齐。列表组间 8px，表单字段组间 12px，面板内容 16px，独立章节 24px。不把编辑器行高、图谱坐标、原生窗口安全区机械替换为间距刻度。

## 5. 字体

使用共享 `--font-sans` 原生系统栈；代码、路径内容、编辑器使用 `--font-mono`。固定字号：辅助 11px、控件/说明 12px、正文/列表标题 13px、分组标题 15px、详情标题 20px。正文 1.45–1.55 行高，代码编辑 1.65；控件垂直居中。常规字重 400、标签 500、标题 600；数字使用等宽数字。避免 9px 状态文字承担关键信息。

## 6. 输入与键盘焦点（强制）

**所有可输入区域不要 outline，也不要用内侧或外扩 box-shadow、聚焦高亮边框模拟 outline。**

覆盖 input（含 number/url/tel 等类型）、textarea、select、可编辑 contenteditable、role=textbox 的实际输入节点，以及其 focus、focus-visible、focus-within 状态。

- 默认和聚焦均 `outline: none`。输入底色使用 `--control-bg`，比所在表面更深。
- Hover 使用 `--control-bg-hover` 和 `--control-border-hover`。
- Focus 通过 `--control-bg-focus` 底色与输入光标反馈；`--control-border-focus` 与默认 `--control-border` 相同，`--control-focus-ring: none`。禁止蓝色或其他高亮焦点描边、内侧阴影、外扩环和光晕。不得引发布局位移。
- 带搜索图标的复合输入，由外壳统一呈现底色、边界和 focus-within；内部 input 背景透明、无独立边界或阴影，避免双层输入框。
- 错误态保留危险边框和文本说明，焦点不能覆盖错误含义。禁用态不能响应 hover/focus 装饰。
- 这条规则不删除按钮、链接、Tab、拖拽分隔条的键盘焦点。非输入控件保留清晰的 focus-visible 指示。禁止全局 `* { outline: none }`。

## 7. 组件状态与动效

所有交互组件覆盖 default、hover、focus-visible、active/selected、disabled；异步操作延续现有 loading/error/success 反馈。选择态使用低饱和蓝色底与文字/图标，状态不能只依赖颜色。按钮不因 hover 缩放或弹跳。

统一快速反馈 160ms、常规切换 220ms，使用已有缓出曲线。遵循 prefers-reduced-motion。错误提示、账户恢复、MCP 开关与更新状态在双主题下共用语义样式。

## 8. 全页面布局方案（2026-09-15）

本节是后续布局改造的目标规范，不代表产品已实现。基于当前深色 1020×680 桌面窗口与页面组件审查。配色、控件尺寸、无输入焦点描边、无可见滚动条继续遵循前文。布局改造应服务于当前工作，而不是把每一页统一包装成同尺寸卡片。

### 8.1 审查范围与现状

| 页面/状态 | 审查依据 | 当前问题与设计处理 |
| --- | --- | --- |
| Skill 本地详情 | 实机与 `App.tsx` | 文档主体方向合理；摘要与原始文档有内容重复，分发入口仅 Logo 不易识别；压缩摘要并明确“管理分发”入口 |
| Skill 远程/仓库详情 | `SkillsThreeColumn` 代码 | 与本地共用详情骨架，来源、版本、安装状态按模式切换，不另起一套 Hero |
| Workflow 流程图 | 实机，当前 93 步流程 | 图谱区域只占上半屏，底部空置，长横向节点难浏览；画布占满剩余高度，提供清晰的视图导航 |
| Workflow 步骤、Skill 预览、本次捕获、独立详情 | 组件与分支代码 | 统一详情标题、工具栏和主体占位；步骤与证据不同时争抢正文宽度 |
| AI 环境全局 | 实机 | Logo、巨型标题、状态分成多行；列表外卡片套行卡片，占用空间；改紧凑标题与平面列表 |
| AI 环境项目 | `AIEnvironmentsThreeColumn` 代码 | 项目身份、环境目标、挂载技能分组；路径和辅助说明不扩大头部 |
| MCP 列表与详情 | 实机与组件代码 | 大块注入矩阵挤到参数配置之前，重复品牌卡片；配置成为主体，分发变紧凑列表 |
| 公共规则库 | 实机与组件代码 | 编辑器与空矩阵接近等权并排；新建入口重复；改全宽编辑，项目分发位于下方 |
| 项目规则 | `RulesThreeColumn` 代码 | 链接助手、规则来源、编译结果需要清晰顺序，不以等大卡片平铺 |
| 账号管理 | 实机空状态与组件代码 | 工具 Tab 占主区顶栏，缺少账号身份层级；常规设置又嵌入完整管理页 |
| 常规、快捷键、权限、关于/更新 | 四页实机与代码 | 基础双栏正确；常规混入完整账号管理；权限入口重复；关于文案偏装饰性 |
| 命令面板、新建/导出/删除、浏览器捕获、MCP/账号弹窗 | 对应组件代码 | 使用统一弹窗骨架，区别短确认和长表单；保持原有危险操作确认、恢复及授权说明 |
| Skill 分发独立窗口、权限引导浮窗 | `SkillLinkManagerWindow.tsx`、`PermisoOverlay.tsx` | 按独立任务窗口设计，保留原生标题栏与拖拽行为，不复制主应用三栏 |

上述实机范围不含所有主题、账号已填充状态、实际授权/更新安装、全部弹窗和子窗口；这些仍属于实施后的验收项。官网不属于桌面布局改造范围，仅检查共享样式兼容。

### 8.2 统一空间结构

**业务页保持三栏：模块导航 → 对象列表 → 当前对象工作区。设置保持双栏。详情区内部默认只设一个主工作面，不再常驻第四栏。**

- Sidebar 160px；Master 默认 210px，保留现有 180–360px 拖拽范围、重置与持久化。拖宽 Master 时优先调整详情内部结构，不压缩输入、隐藏按钮或省略整列。
- 当前账号页面是两栏实现，属于现状差异；目标按 AGENTS.md 业务三栏规范组织，账号的 Master 结构见 §8.8。不得把当前例外固化为通用规范。
- 详情外层唯一负责 38px 顶部安全距离、16px 水平和24px底部内边距。设置为38/28/40px。内部页面容器不得再次叠加整页 padding。
- 详情左边缘统一对齐标题、工具栏和正文；普通详情不使用自动居中造成起点漂移。文档正文可限阅读行长，编辑器、图谱和列表利用可用宽度。
- 通用头部按“对象名称 + 操作”首行、“简短描述/路径”次行组织。标题20px，Logo20px，不使用巨型 Hero。首行最小28px且允许增高；标题最多显示两行，完整名称仍能读取。
- 头部与工具栏相隔12px，工具栏与主体相隔16px；字段组12px，独立分区24px。工具栏无边框大卡片包装。
- 主动作最多一个，例如保存、安装、添加。删除、移除、停用使用次级或更多操作区，保留必要确认；不得与主动作使用相同蓝色权重。
- 描述默认最多两行，长说明提供展开；错误和影响当前操作的重要提示不得折叠隐藏。实际对象名称、来源、状态与提示使用不同层级，避免堆徽章。

### 8.3 Master 与详情状态

- Master 从上到下：一行范围 Tab → 搜索（有创建功能时同行放一个创建入口）→ 对象列表。Tab24px、搜索28px，间隔8px。无范围切换的页面不为对齐虚构 Tab。
- 列表名称13px，辅助信息11–12px，默认一行名称加至多一行必要状态；单行对象列表行最小36px，双行最小48px。该场景规则细化 §4 的普通列表默认值。
- 中栏禁止叠加第二层过滤条、冗余总数徽章或重复创建入口；状态只有在帮助选择对象时才进入列表。
- 空列表保留顶部唯一创建入口，空说明不再复制同一按钮。没有选中对象时显示简短引导，不自动展示一个仿佛正在编辑的空表单。用户显式新建后才显示草稿界面。
- 搜索无结果、尚未创建、加载失败、没有选择对象是四种不同状态；分别提供清空搜索、创建入口指引、重试和选择指引。
- 切换对象时按既有规则处理未保存内容；搜索词、选中项、滚动位置按模块保留。布局调整不得吞掉保存/取消或错误反馈。

### 8.4 Skill：文档优先

- 本地/远程共用“名称与动作 → 摘要/来源 → 文档工具栏 → SKILL.md”结构。阅读与编辑使用同一主工作区；保留现有支持的操作，不凭设计方案新增自动保存。
- 本地明确显示“管理分发”，环境 Logo 是补充识别，不作为唯一入口。删除置于次级操作。远程主动作显示实际安装/更新状态，来源与版本放摘要行。
- 文档占详情全宽；渲染正文阅读行长65–75ch，代码块与编辑器不受正文行长限制。原始 frontmatter 不应再抄成额外卡片；必要元数据集中呈现一次。
- 分发管理继续复用现有独立窗口/入口，不常驻占据正文右侧。未选择与未安装状态不以巨大空框占位。

### 8.5 Workflow：画布与步骤优先

- 标题行显示当前流程名称及现有保存/导出动作；其下保留“流程图 / 步骤编辑 / Skill 预览”视图切换，三个视图共享相同主体边界。
- 流程图填满工具栏以下的剩余高度，常规680px窗口目标可用画布至少360px。禁止固定一张约300px高的图卡并留下整片空白。
- 长流程必须有可发现的浏览方式：平移/缩放、适应画布、定位选中节点；无可见滚动条仍须支持键盘到达节点。超长链路按拓扑正常展开，不能用裁切掩盖后续步骤。
- 选中节点摘要使用画布底部紧凑信息条（内容自适应，通常48–72px）。“编辑步骤”进入现有步骤编辑视图并定位，不另开常驻右栏挤压画布。
- 步骤视图纵向组织，步骤选择和字段编辑在同一主体中顺序呈现；长参数与说明全宽。Skill 预览采用文档模板，不保留图谱空白占位。
- 本次捕获顶部仅放捕获状态及已有控制，事件列表在 Master，右侧显示选中事件和证据。空状态说明下一步，不同时铺开完整教程与空证据卡片。只设计观察、审阅和导出，不新增自动执行电脑操作。
- 独立流程详情复用上述身份、动作和图谱结构；保留返回入口，不再使用另一套浮动大卡片框架。

### 8.6 AI 环境：挂载列表优先

- 全局环境头部一行“官方 Logo + 纯净名称 + 状态”，下一行放路径；通用环境显示 `agents`。标题20px，不拆成 Logo、巨型名字、范围、操作四层。
- 批量挂载和打开目录放同一工具栏。批量操作保留原有业务边界、结果反馈和确认要求。
- 详情的 Skill 搜索直接属于挂载列表；列表使用平面行，名称/简短说明在左，挂载状态及行操作在右。取消“外层卡片 + 每行卡片”的重复边界。
- 数量仅在列表标题旁展示一次；环境已选中态和某个 Skill 已挂载态不能用同样整块高亮混淆。
- 项目模式由 Master 选择项目，详情显示项目路径、目标环境和挂载列表；目标环境可以在正文内紧凑选择，不新增业务侧栏。不存在的目标目录明确标注待创建。

### 8.7 MCP 与规则：编辑优先，分发次之

**MCP**

- 顺序固定为：服务名称/启用状态/保存动作 → 简短生效提示 → 参数配置 → 应用到环境/项目。
- 命令或 URL、参数、环境变量、请求头是主配置区，按传输类型显示实际所需字段。命令、URL、多行参数、键值列表默认全宽；仅短且相关的字段可并排。
- 分发区使用紧凑行：“环境名称 / 状态 / 路径 / 操作”。已注入状态用文字和小标记，不给整张环境卡片涂高饱和底色。不默认显示巨型双列注入矩阵。
- 分发标题可显示摘要并展开管理；未添加项目时仅一行说明与添加入口，不设高空框。配置修改影响目标的说明紧邻保存与分发区，避免用户遗漏生效范围。

**公共规则库**

- 顶部名称、描述明确关联标签；保存位于头部右侧。名称与描述纵向排列，正文工具栏只保留必要模式和状态。
- Markdown 编辑器占满详情宽度，不与项目矩阵并排。编辑器在680px高窗口中默认至少320px，空间足够时增高；不得为下方空分发区保留半屏。
- “应用到项目”放在编辑器下方，默认显示数量/状态摘要及展开入口；展开后使用项目行。空态无最低大高度，不建立嵌套滚动卡片。
- 页面外层负责访问正文后的分发区域，编辑器内部负责长文本滚动；不得固定编辑器至使下方内容无法访问。

**项目规则**

- 项目名称与路径在头部，打开目录/移除是次级动作。正文顺序为：需要处理的链接状态 → 已关联规则列表 → 编译结果/预览。
- 链接助手正常时收为一行，冲突时展开原因与修复动作；不能把正常状态做成占首屏的大卡片。预览是辅助内容，默认按需展开。

### 8.8 账号：一个管理入口

- 目标三栏：Sidebar → Master 中提供商切换及该提供商账号列表 → 选中账号详情。Provider Tab 保持紧凑三项，长名称有可读标签；账号列表优先身份名称与当前状态。
- 账号详情顺序为：身份与当前使用状态 → 额度/刷新时间 → 切换与恢复信息 → 必要配置详情。无账号时使用简短引导和唯一添加入口；认证方式在添加流程内选择，避免与顶栏重复主动作。
- 额度在宽内容区允许并排比较，窄区改纵向；未知额度明确显示未知，不用空图卡撑高度。
- 同一认证冲突只显示一个就近提示；详细技术原因可展开。切换、备份、回滚和恢复的已有提示与保护不得因简化布局删除。
- 常规设置不再嵌入完整账号工作台，只保留一行“账号管理”及跳转入口；账号实际业务逻辑仍由同一模块负责。

### 8.9 设置：短分组与自然留白

- 统一双栏；右侧内容左对齐、最大宽度960px，不因某页内容少而居中浮动。页标题与首组间24px；每组内使用平面设置行，禁止卡片再套卡片。
- 行结构为左侧标签/解释、右侧控件；默认最小56px，长说明自然增高。长路径可换行，动作不被挤成逐字换行。内容宽度不足600px时控件移到标签下方。
- 常规分“外观与语言”“存储与工作区”“账号入口”。迁移操作独立于普通路径选择，影响说明始终可见，保留原有确认。
- 快捷键按功能分组，左侧动作右侧键帽；只有两项时自然结束，不为填满高度增加卡片或空行。
- 权限页顶部摘要说明缺少哪些权限，各权限行显示用途、状态和相关操作；总入口与行入口不重复抢占蓝色主操作权重。保留系统授权流程。
- 关于页紧凑显示应用名、版本、简短用途，更新区紧随其后；删除纯装饰性设计术语。更新检查、进度、失败重试及可安装状态在同一位置切换，不改变升级逻辑。

### 8.10 弹窗与独立窗口

- 通用弹窗采用“标题/关闭 → 内容 → 取消/主动作”三段。短确认目标宽400px、普通表单520px、树形管理720px，实际宽度不得超过窗口宽度减32px；最大高度为窗口高度减48px。
- 只有中间内容滚动，标题与底部动作可达且不遮挡字段。短内容按实际高度收缩；不得为了统一尺寸保留空白大框。
- 新建 Skill/MCP、导出、浏览器捕获使用相同字段间距和标签位置；账号 OAuth/导入按现有流程展示状态，不把授权说明折叠隐藏。删除影响清单始终可访问。
- 命令面板用搜索 + 分组结果 + 简短键盘提示，不套普通表单底栏；ESC和焦点返回行为保持一致。
- Skill 分发独立窗口用原生标题区 → 全局/项目 → 环境或项目树 → 必要状态/动作。空树按内容收缩，长树只由主体滚动。
- 权限引导浮窗围绕操作说明和系统拖拽目标组织，保留返回与拖拽热区；不得添加主导航、装饰 Hero 或抢占系统权限窗的大片遮罩。

### 8.11 窄栏与滚动规则

断点以**详情可用内容宽度 D**计算，不以整个窗口宽度代替；Master 拖宽同样触发调整。

| 条件 | 布局要求 |
| --- | --- |
| D ≥ 800px | 短字段/额度可双列；文档、规则与主图谱仍全宽，分发仍在下方 |
| 600px ≤ D < 800px | 主体单列为默认，仅经验证的短字段可并排 |
| D < 600px | 所有表单单列，工具栏允许换行，行操作移到内容下一行；不显示常驻辅助列 |
| 窗口较矮 | 保留顶部安全区，减少非必要描述，主体可滚动；不得用固定 min-height 撑出无法访问的底部 |

- Header、Master 顶部控件固定于各自区域；普通详情由一个外层容器滚动。只有编辑器、图谱和真正独立的长列表允许额外滚动，不让每张卡片都有独立滚动。
- 文本、错误、按钮必须自然换行；长路径可断行，状态短语和按钮文字不得逐字断裂。页面不产生横向滚动，代码和图谱按其必要交互支持横向浏览且不显示滚动条。
- 最低验证窗口780×600、常规1020×680、宽屏1440×900，分别检查Master默认210px及拖宽360px。780px窗口下详情最窄约228px，必须仍能纵向访问字段和操作；该尺寸是验收目标，不是已通过声明。

## 9. 后续实施顺序与验收门槛

本次只更新文档。后续产品代码改造仍按 AGENTS.md 登记需求、取得实施授权；本文不是已完成实现记录，也不修改既有业务权限。

1. **统一骨架**：抽取/复用详情头部、工具栏、平面列表、空状态、设置行与弹窗骨架；只由一个容器拥有页面 padding。保持原有数据和选中状态。
2. **优先修正空间分配**：公共规则全宽编辑、MCP 配置前置、Workflow 画布高度、AI 环境紧凑头部及平面列表。
3. **统一信息归属**：账号三栏与设置入口去重，完善远程 Skill、项目环境/规则和捕获状态。
4. **补齐边界**：全部弹窗、独立窗口、长文本、错误恢复、窄栏、双主题与键盘导航。

每页交付须记录：改造前后截图、实际窗口/详情宽度、对应规范条款、已验证状态、未覆盖状态。每个一级页面至少验证空/有数据、长名称、错误、加载、未保存（适用时）；所有设置子页与弹窗逐项记录。验证码、账号切换、系统授权、迁移和安装等不能为布局验收自动触发，未触发状态按代码审查如实标注。

验收核心：当前对象身份清晰；主动作可找到；主要编辑/图谱空间足够；辅助信息不抢首屏；没有重复入口、嵌套卡片、不可访问内容、可见滚动条或输入焦点描边。不得仅凭某个控件像素正确或构建通过，就宣称全页符合设计。

## 10. 工程验收基线

1. 优先修正共享 token 和原始组件规则，禁止在文件末尾不断追加高优先级补丁；模块只保留布局与必要场景差异。
2. 核对 Skill、工作流、AI 环境、MCP、账号、设置/更新、搜索与编辑弹窗。不得借样式修复修改 IPC、账号逻辑、协议或发布行为。
3. 浅色/深色各核对常规窗口与窄窗口；检查 26/24/22/28px 控件、Tab 不溢出、长文本及滚动。横向与纵向滚动条在默认、悬停、聚焦、滚动中均不可见；覆盖系统“始终显示滚动条”的设置，检查无空槽、无内容裁切，并验证滚轮、触控板、键盘及编辑光标滚入视口正常。
4. 鼠标与 Tab 聚焦输入均无 outline/外扩环，不出现内侧阴影或高亮边框，焦点通过底色和光标反馈；复合搜索只有一层背景/焦点；非输入控件键盘导航可见。
5. 运行 pnpm typecheck 与 pnpm build，审查 diff。视觉检查与未覆盖项必须如实记录，不以构建成功代替视觉验收。
6. Antigravity 提供核对和修复结果，由主 Agent 审阅、验证、在 main 提交，并更新对应 Linear 需求。
