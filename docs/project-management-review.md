# 项目管理实施与验证（OPC-64）

2026-09-15。范围：独立项目管理入口，无搜索，作为 MCP / Skill / 公共规则共用的项目基础。

## 结果

- 新增三栏项目页，显示名称、路径、有效状态、添加时间与添加/设为当前/访达/移除操作。详情不加入重复元信息卡、注入说明卡或技能探测矩阵。
- 管理接口保留失效目录；原 `listProjects` 仍只返回有效目录，原有注入消费者无需切换数据源。统一使用现有项目记录、存储和变更通知。
- 项目变化、手动刷新及窗口重新聚焦时刷新；加载与失败状态可见，异步旧结果不会覆盖新请求。
- 移除仅移出管理列表；不会删除源码和已注入内容。原生 dialog 提供模态焦点约束与 Escape 取消，确认目标固定为打开时的项目。
- 补修旧项目按路径移除后被 legacy projectWorkspace 重新迁回的问题；移除当前项目后只回退到有效目录。
- DESIGN.md §8.12 记录后续遵守的项目页规范。

## 协作

Antigravity 首轮读取阶段触及 600 秒时限；续跑产出共享类型、数据层、IPC、文案及页面初稿。主 Agent 审阅发现初稿额外卡片/探测内容、错误反馈和 API 使用问题，停止续跑后完成精简、导航接入、样式、回归测试及验证。未将 Antigravity 初稿或任务启动视为最终完成。

## 验证

- pnpm typecheck：5/5 通过。
- pnpm build：5/5 通过；最后修正后桌面 build 再次通过。
- verify-opc64-project-management：临时目录验证目录去重、稳定标识、落盘重读、设为当前、失效目录、文件路径拒绝、移除保留源码与规则、legacy 项目移除不复活。
- verify-opc56-unified-injection：12 组既有多项目注入回归通过。
- verify-opc58-ui-system、git diff --check：通过。
- 真实 Trace 1020×680 深色、Master 311px：项目管理导航可达、空状态无搜索、单一添加入口；原生目录选择器打开与取消正常，未新增用户项目。
- 未实机覆盖有数据详情、移除弹窗、全窗口尺寸和浅色主题；这些状态经过代码审阅，数据行为由临时目录测试验证，不声称全状态视觉验收通过。系统目录选择器使用系统原有外观；页面自身不添加搜索或可见滚动条。
- 构建仍有既有 bundle 超 500kB 和 typecheck 型构建无产物警告，不影响本次通过结果。
