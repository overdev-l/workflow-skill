# 全页面布局改造验收记录

日期：2026-09-15。依据：DESIGN.md 第 8–10 节。关联：OPC-60。

## 实施

由 Antigravity 执行页面改造，主 Agent 审阅并修正 JSX/类型问题、容器查询层级、规则刷新选择状态、图谱缩放/定位/循环数据处理、步骤编辑窄栏溢出、AI 环境嵌套卡片和账号重复错误提示。保留业务三栏、设置双栏、现有配色、无输入 outline 和隐藏滚动条规则。

- Skill：详情文档占据主区域，分发管理按需打开，长描述可展开，移除伪造的详情版本标签。
- Workflow：标题/动作/视图切换统一；图谱占用剩余高度，支持缩放、适应、定位、拖拽与键盘选点；窄详情区步骤列表与编辑器上下排列。
- AI 环境：紧凑头部、路径与动作，技能使用平面列表。
- MCP：参数配置前置，分发默认折叠，窄栏参数及目标行纵向排布。
- 公共规则：全宽 Markdown 编辑器、字段标签、单一新建入口、分发与预览按需展开；切换规则保存内存草稿，后台刷新保持当前选择。
- 账号：供应商/账号列表/详情三栏，详情身份及动作去重，保留授权错误、当前身份、恢复、回滚和安全说明。
- 设置：外观语言、存储工作区、账号入口分组；权限操作分主次；关于页缩短说明。
- 弹窗：统一宽高上限及正文滚动；独立分发窗口正文可滚动。

## 实机观察

使用本地 Electron 开发窗口。主要观察尺寸为 **1020 × 680**，中间栏 **311px**，详情区约 **549px**（内容区约 517px）。没有改变用户保存的中间栏宽度。截图观察见本次会话；账号、MCP 页面可能含私人数据，未将截图提交仓库。

| 页面/状态 | 本次观察结果 |
| --- | --- |
| 本地 Skill，有数据、长描述、文档 | 主文档全宽，管理分发/导出/删除可见，无可见滚动条 |
| 远程 Skill，已有列表及详情 | 头部动作换行，文档可读；未执行安装 |
| 工作流，93 步数据 | 适应画布实际缩至约 2.2%，定位选中步骤恢复 100% 并显示节点 |
| 工作流步骤编辑 | 窄栏上下排列，滚动可到说明与末尾操作；右侧不再裁切 |
| 工作流 Skill 预览 | 原预览内容与动作仍可访问 |
| 本次捕获，空态与开始弹窗 | 信息与开始入口可见，URL 输入聚焦无额外描边；打开弹窗后取消，未启动捕获 |
| AI 环境，全局有数据 | 平面列表，头部及动作适应窄详情区 |
| MCP，有数据配置页 | 配置位于分发之前，参数可编辑；未保存、注入或删除 |
| 公共规则，空态与新建草稿 | 空态明确；编辑器全宽，鼠标聚焦无额外 outline/焦点环；未保存测试规则 |
| 账号，空供应商与已有账号 | 三栏、短供应商 Tab、详情单组动作；已有 Keychain 拒绝提示保留，未授权或切换账号 |
| 设置四个子页 | 双栏、自然内容高度、通用分组与账号跳转入口；浅色/深色通用页均观察，已恢复深色 |

开发窗口曾在连续热更新后空白，控制台未出现渲染错误；重启开发进程后恢复并完成上述检查。该现象未获得稳定复现，不能据此认定根因已消除。

## 自动验证

- `pnpm typecheck`：通过。
- `pnpm build`：桌面和 Web 均通过；保留原有包体积提示。
- `node scripts/verify-opc58-ui-system.mjs`：通过布局、主题、控件高度、无双栏回退及动效检查。
- `node scripts/verify-workflow-graph.mjs`：实际组件渲染验证 93 节点长链、单一键盘入口、分支高度、循环数据及空数据。
- `git diff --check`：通过。

## 尚未覆盖的人工状态

本次未完成 780×600、1440×900 及中间栏 210/360px 的完整组合矩阵；1020×680 下详情宽度已小于 600px，可验证容器查询实际生效。未穷举每个页面的网络加载、错误、全部长名称、空/满数据、全部弹窗和独立权限窗口。项目环境/项目规则分发及独立窗口以代码审查为主。账号授权/切换/恢复、迁移、删除、安装和更新没有为布局验收触发；这些业务行为不能视为本轮实机测试通过。键盘及图谱拖拽已有实现和代码审查，未穷举全部输入方式。

后续按 DESIGN.md 增补这些人工矩阵；本记录只代表上述已验证范围，不替代完整交互验收。

## 账号页后续纠正（2026-09-15）

用户复验后明确否定账号三栏布局。本节与最新 DESIGN.md §8.8 覆盖上文账号三栏方案；其他页面保持本轮改造。

- 恢复 Sidebar + 全宽账号工作台：工具 Tab、账号卡片网格和原有账号动作，不设中间列表或列宽调整条。
- 切换工具自动刷新该工具下账号配额，近期正常缓存不再阻止刷新；保留并发、队列/in-flight 去重和失败冷却。工具 Tab 切换不等于切换真实账号。
- DESIGN.md 与 AGENTS.md 固化该例外，保留输入无 outline 和滚动条不可见约束。
- 新增 `scripts/verify-account-tool-refresh.mjs`，在隔离环境执行实际组件的调度回调与 effect，覆盖缓存 A→B→A、稳定重渲染、队列去重、in-flight、失败冷却与空账号场景；不调用真实 API。

本次实施由主 Agent 完成：Antigravity 读取组件后较长时间未产出修改，已向用户说明并取消该委派。实机确认 1020×680 下账号页无 Master/拖拽条，Claude 空态与 Antigravity 多账号卡片正常；切换工具后保留原有凭据过期/Keychain 拒绝状态，未触发新的系统授权或真实账号切换，不能声称获取到了有效的新配额。调度行为由实际回调隔离测试验证。

验证结果：`pnpm typecheck`、`pnpm build`、UI 规范检查、新增工具切换刷新回归均通过；既有 `verify-account-quota.mjs` 25 项全部通过，`git diff --check` 通过。开发窗口已通过热更新显示本次代码。
